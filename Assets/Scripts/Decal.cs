using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Decal : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // GetComponent<Material>().renderQueue = 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
